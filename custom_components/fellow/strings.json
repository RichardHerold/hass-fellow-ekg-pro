{
  "config": {
    "step": {
      "user": {
        "title": "Set up Fellow Stagg EKG",
        "description": "Add your kettle by entering its IP address (find it in your router's client list), or scan the local network for kettles.",
        "menu_options": {
          "manual": "Enter IP address",
          "pick": "Scan network"
        }
      },
      "pick": {
        "title": "Scan for kettles",
        "description": "Choose which network to scan. The field is prefilled with a network Home Assistant is connected to — if your kettle is on a different subnet or an IoT VLAN, enter that network instead (e.g. 192.168.20.0/24, or just an IP on it to scan its /24). Scanning another VLAN only works if your firewall lets Home Assistant reach it over HTTP.",
        "data": {
          "network": "Network to scan"
        }
      },
      "pick_device": {
        "title": "Kettles found",
        "description": "Select a discovered kettle.",
        "data": {
          "host": "Kettle",
          "temperature_unit": "Temperature Unit"
        }
      },
      "manual": {
        "title": "Set up Fellow Stagg EKG",
        "description": "Enter the IP address of your kettle and select your preferred temperature unit for Home Assistant.",
        "data": {
          "host": "IP Address",
          "temperature_unit": "Temperature Unit"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the kettle. Please check the IP address and ensure the kettle is powered on and connected to your network.",
      "invalid_network": "Enter a network like 192.168.20.0/24 (no larger than /22) or an IP address on it.",
      "no_kettles_found": "No kettle answered on that network. Check that you scanned the subnet the kettle is on (see your router's client list), or go back and add it by IP address instead.",
      "incomplete_response": "The kettle answered, but its response could not be fully understood, so setup was stopped (the temperature entities would not work). Make sure the kettle is sitting on its base and try again. If it keeps failing, check the Home Assistant log for the raw response and run examples/probe_kettle.py — your firmware may format responses differently.",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Fellow Stagg EKG Options",
        "description": "Configure your kettle settings",
        "data": {
          "temperature_unit": "Temperature Unit",
          "temp_set_method": "Temperature-setting method",
          "poll_interval": "Idle poll interval (seconds)",
          "active_poll_interval": "Active poll interval (seconds)",
          "sync_kettle_units": "Sync unit to the kettle's display",
          "presets": "Temperature presets",
          "sync_clock": "Keep the kettle's clock synced"
        },
        "data_description": {
          "temperature_unit": "The unit Home Assistant uses for this kettle",
          "temp_set_method": "Direct uses firmware set-target commands with verification (fast). Dial emulates the physical dial (slower but works on firmwares without a set-target command).",
          "poll_interval": "How often to read the kettle's state while it's off (5-60 seconds)",
          "active_poll_interval": "How often to read the kettle's state while it's heating or keeping warm (2-60 seconds) — faster polling keeps the temperature and Water Ready fresh",
          "sync_kettle_units": "When enabled, changing the temperature unit here also changes the unit shown on the kettle's own display",
          "presets": "One-tap heat buttons as 'Name: temperature' pairs separated by commas, in your configured unit — e.g. 'Pour-over: 96, Green tea: 79'. A Boil button is always included.",
          "sync_clock": "When enabled, the kettle's display clock is set to Home Assistant's local time on startup and once a day (handles drift and DST). A manual Sync Clock button is always available."
        }
      }
    },
    "error": {
      "invalid_presets": "Presets must be 'Name: temperature' pairs separated by commas, within 40-100 °C (104-212 °F), with unique names — e.g. 'Pour-over: 96, Green tea: 79'."
    }
  },
  "entity": {
    "sensor": {
      "kettle_state": {
        "name": "Kettle State",
        "state": {
          "off": "Off",
          "heating": "Heating",
          "at_temperature": "At temperature",
          "keeping_warm": "Keeping warm"
        }
      }
    }
  },
  "selector": {
    "temperature_unit": {
      "options": {
        "celsius": "Celsius (°C)",
        "fahrenheit": "Fahrenheit (°F)"
      }
    },
    "temp_set_method": {
      "options": {
        "direct": "Direct (recommended)",
        "dial": "Dial emulation (fallback)"
      }
    }
  }
}
