{
  "config": {
    "step": {
      "user": {
        "title": "Set up Fellow Stagg EKG",
        "description": "Add your kettle by entering its IP address (find it in your router's client list), or scan the local network for kettles.",
        "menu_options": {
          "manual": "Enter IP address",
          "pick": "Scan network"
        }
      },
      "pick": {
        "title": "Kettles found on your network",
        "description": "Select a discovered kettle.",
        "data": {
          "host": "Kettle",
          "temperature_unit": "Temperature Unit"
        }
      },
      "manual": {
        "title": "Set up Fellow Stagg EKG",
        "description": "Enter the IP address of your kettle and select your preferred temperature unit for Home Assistant.",
        "data": {
          "host": "IP Address",
          "temperature_unit": "Temperature Unit"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the kettle. Please check the IP address and ensure the kettle is powered on and connected to your network.",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Fellow Stagg EKG Options",
        "description": "Configure your kettle settings",
        "data": {
          "temperature_unit": "Temperature Unit",
          "temp_set_method": "Temperature-setting method",
          "poll_interval": "Poll interval (seconds)",
          "sync_kettle_units": "Sync unit to the kettle's display"
        },
        "data_description": {
          "temperature_unit": "The unit Home Assistant uses for this kettle",
          "temp_set_method": "Direct uses firmware set-target commands with verification (fast). Dial emulates the physical dial (slower but works on firmwares without a set-target command).",
          "poll_interval": "How often to read the kettle's state (5-60 seconds)",
          "sync_kettle_units": "When enabled, changing the temperature unit here also changes the unit shown on the kettle's own display"
        }
      }
    }
  },
  "selector": {
    "temperature_unit": {
      "options": {
        "celsius": "Celsius (°C)",
        "fahrenheit": "Fahrenheit (°F)"
      }
    },
    "temp_set_method": {
      "options": {
        "direct": "Direct (recommended)",
        "dial": "Dial emulation (fallback)"
      }
    }
  }
}
